---
phase: 08
plan: 03
subsystem: lex-web AI endpoint
tags: [typescript, nextjs-16, anthropic, sonnet, streaming, rate-limit, cache, supabase]
wave: 2
depends_on: [08-01]
files_modified:
  - /Users/beyond/Desktop/lex-web/app/api/dv/summarize/route.ts
  - /Users/beyond/Desktop/lex-web/__tests__/dv-summarize-route.test.ts
task_count: 2
autonomous: true
requirements: [DV-02]
objective: |
  Build the per-act AI summary endpoint POST /api/dv/summarize: Anthropic Sonnet 4.6 streaming, citizen-friendly
  Bulgarian markdown, rate-limited 10/min/IP, signal: req.signal propagation (AI-07 preserved), DB write-back
  cache to dv_acts.summary_ai (write AFTER stream completes, NEVER in finally — partial summaries poison).
  Wave 2 parallel-safe with plan 08-02; zero shared file modifications.
success_criteria:
  - app/api/dv/summarize/route.ts exists with runtime: "nodejs", maxDuration ≤ 60, rateLimited("dv-summarize", { windowMs: 60_000, max: 10 })
  - Model hardcoded to "claude-sonnet-4-6"; signal: req.signal forwarded to client.messages.stream
  - Cache-hit path: SELECT summary_ai from dv_acts; if non-null, faux-stream the cached value with X-Source: cache header (instant)
  - Cache-miss path: stream Anthropic response, accumulate, write-back AFTER stream loop completes inside try, NEVER in finally
  - Response shape: Content-Type: text/plain; charset=utf-8; Cache-Control: no-store
  - 6+ vitest cases pass covering: rate-limit 429, model identity grep, signal propagation grep, cache hit short-circuit, cache miss write-back, abort no-poison
  - bunx tsc --noEmit + bun run build clean
---

<objective>
Build the public-facing AI summary endpoint for the Държавен вестник browser. Per-act citizen-friendly markdown explanation in Bulgarian, served via streaming Anthropic Sonnet 4.6, with DB write-back caching to amortize cost across readers.

This plan is Wave 2 parallel-safe with plan 08-02 — they share lib/queries.ts (08-02 modifies it, 08-03 does not) and no other files. The endpoint is consumed by 08-02's DvActSummary client component, but typescript compiles both independently.

Critical correctness invariant (RESEARCH §Q6): the cache write-back MUST happen AFTER the for-await-of loop completes successfully, INSIDE the try block. NEVER in finally (finally runs on abort, leaving a partial summary cached — poison). The catch block must NOT write-back. This is the single highest-risk implementation detail in this plan.

Phase 1 carry-forward: `rateLimited(req, key, opts)` server gate + `hashIp(ip)` HMAC ip_hash + canonical structured throttle log line (D-09/D-10) all reused as-is.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: app/api/dv/summarize/route.ts (Anthropic Sonnet streaming POST endpoint with DB write-back cache)</name>
  <files>/Users/beyond/Desktop/lex-web/app/api/dv/summarize/route.ts</files>
  <read_first>
    - /Users/beyond/Desktop/lex-web/.planning/phases/08-dv-gazette/08-RESEARCH.md §"Q6 — AI summary cache write-back: stream-completion detection" (the VERBATIM TypeScript pattern; do NOT paraphrase — copy the try-after-loop / never-finally structure exactly)
    - /Users/beyond/Desktop/lex-web/.planning/phases/08-dv-gazette/08-CONTEXT.md D-13, D-14 (route shape + rate-limit + Sonnet 4.6 + full-text input)
    - /Users/beyond/Desktop/lex-web/app/api/intel/search/route.ts (CANONICAL streaming pattern — `runtime: "nodejs"` + rate-limit + Anthropic + `ReadableStream` + `client.messages.stream({...}, { signal: req.signal })`)
    - /Users/beyond/Desktop/lex-web/app/api/eu/summarize/[celex]/route.ts (closest per-record summarize precedent — same overall shape)
    - /Users/beyond/Desktop/lex-web/lib/rate-limit.ts (Phase 1 `rateLimited(req, key, opts)` signature)
    - /Users/beyond/Desktop/lex-web/lib/supabase.ts (anon-key client; sufficient for read; write to summary_ai needs service-role client — see action below)
  </read_first>
  <action>
    Create `/Users/beyond/Desktop/lex-web/app/api/dv/summarize/route.ts`. Full source (copy structure verbatim from RESEARCH §Q6, model is `claude-sonnet-4-6`, route key is `dv-summarize`, rate limit 10/min):

    ```typescript
    import Anthropic from "@anthropic-ai/sdk";
    import { createClient } from "@supabase/supabase-js";
    import { rateLimited } from "@/lib/rate-limit";

    export const runtime = "nodejs";
    export const maxDuration = 60;

    const SYSTEM_PROMPT = `Ти си експерт по българско право, който обяснява закони и подзаконови актове на обикновени граждани.

Получаваш заглавието и пълния текст на акт от Държавен вестник. Отговаряй на български в кратък markdown формат:

## Какво прави този акт
[1–2 изречения. Кой го издава, какво променя или регулира, кога влиза в сила.]

## Засегнати лица
[До 3 точки със "- " — конкретни групи, индустрии или ситуации, за които има значение.]

## Ключови разпоредби
[До 4 точки със "- " — основните задължения, права или промени. Цитирай номера на членове или параграфи, ако са в текста.]

ВАЖНО: Не измисляй съдържание. Ако нещо не е ясно от текста, кажи "не е уточнено". Не давай правни съвети — само обяснение.`;

    type RequestBody = { actId?: string };

    /**
     * Service-role Supabase client for write-back.
     * Falls back to anon client for the cache-read; only the write needs service role
     * (anon RLS may prevent updating dv_acts.summary_ai depending on policy).
     */
    function getServiceSupabase() {
      const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
      const key = process.env.SUPABASE_SERVICE_ROLE_KEY ?? process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
      if (!url || !key) throw new Error("Supabase env vars missing");
      return createClient(url, key, { auth: { persistSession: false } });
    }

    export async function POST(req: Request) {
      // 1. Rate-limit gate
      const limit = rateLimited(req, "dv-summarize", { windowMs: 60_000, max: 10 });
      if (limit) return limit;

      // 2. Parse + validate body
      let body: RequestBody;
      try {
        body = (await req.json()) as RequestBody;
      } catch {
        return new Response("Invalid JSON", { status: 400 });
      }
      const actId = (body.actId ?? "").trim();
      if (!actId) return new Response("Празна заявка (actId)", { status: 400 });

      // 3. Fetch act from DB
      const supabase = getServiceSupabase();
      const { data: act, error: actErr } = await supabase
        .from("dv_acts")
        .select("id, title, act_type, full_text, summary_ai")
        .eq("id", actId)
        .limit(1)
        .single();

      if (actErr || !act) {
        return new Response("Актът не е намерен", { status: 404 });
      }

      if (!act.full_text || act.full_text.length < 20) {
        return new Response("Актът няма достатъчно съдържание за обобщение", { status: 422 });
      }

      // 4. Cache hit — faux-stream the cached value
      if (act.summary_ai && act.summary_ai.length > 0) {
        const encoder = new TextEncoder();
        const cached = act.summary_ai;
        const cacheStream = new ReadableStream({
          start(controller) {
            controller.enqueue(encoder.encode(cached));
            controller.close();
          },
        });
        return new Response(cacheStream, {
          status: 200,
          headers: {
            "Content-Type": "text/plain; charset=utf-8",
            "Cache-Control": "no-store",
            "X-Source": "cache",
          },
        });
      }

      // 5. Cache miss — call Anthropic and write-back AFTER stream completes
      const client = new Anthropic();
      const encoder = new TextEncoder();
      let collected = "";

      const stream = await client.messages.stream(
        {
          model: "claude-sonnet-4-6",
          max_tokens: 1024,
          system: SYSTEM_PROMPT,
          messages: [
            {
              role: "user",
              content: `ЗАГЛАВИЕ: ${act.title}\nТИП АКТ: ${act.act_type ?? "Неопределен"}\n\nПЪЛЕН ТЕКСТ:\n${act.full_text}`,
            },
          ],
        },
        { signal: req.signal },
      );

      const responseStream = new ReadableStream({
        async start(controller) {
          try {
            for await (const event of stream) {
              if (event.type === "content_block_delta" && event.delta.type === "text_delta") {
                const chunk = event.delta.text;
                collected += chunk;
                controller.enqueue(encoder.encode(chunk));
              }
            }
            // STREAM COMPLETED CLEANLY — write-back HERE, inside try, AFTER the loop.
            // Do NOT move this to finally — finally runs on abort, would poison the cache with partial text.
            if (collected.length > 0) {
              await supabase
                .from("dv_acts")
                .update({
                  summary_ai: collected,
                  summary_ai_generated_at: new Date().toISOString(),
                })
                .eq("id", actId);
            }
            controller.close();
          } catch (err) {
            // Aborted or upstream error — DO NOT write-back. Partial summary is poison.
            controller.error(err);
          }
        },
      });

      return new Response(responseStream, {
        status: 200,
        headers: {
          "Content-Type": "text/plain; charset=utf-8",
          "Cache-Control": "no-store",
          "X-Source": "fresh",
        },
      });
    }
    ```

    Key invariants verified by the grep gates in acceptance:
    - `runtime = "nodejs"` (NOT edge — Anthropic SDK incompatible with edge runtime)
    - `maxDuration = 60` (matches Phase 2 puppeteer ceiling pattern)
    - `rateLimited(req, "dv-summarize", { windowMs: 60_000, max: 10 })` — strict 10/min per CONTEXT D-13
    - `model: "claude-sonnet-4-6"` literal string (NOT a variable that could drift)
    - `signal: req.signal` second-arg to `client.messages.stream` (AI-07 preservation)
    - Write-back inside `try`, AFTER the for-await-of loop, gated by `if (collected.length > 0)`
    - Catch block does NOT call `.update()` — only `controller.error(err)`
    - No `finally` block anywhere in the route handler
    - `Content-Type: text/plain; charset=utf-8`, `Cache-Control: no-store`, `X-Source: cache|fresh` header for client visibility
  </action>
  <acceptance_criteria>
    - File `/Users/beyond/Desktop/lex-web/app/api/dv/summarize/route.ts` exists
    - `grep -c 'export const runtime = "nodejs"' app/api/dv/summarize/route.ts` returns 1
    - `grep -c 'export const maxDuration' app/api/dv/summarize/route.ts` returns 1
    - `grep -c '"claude-sonnet-4-6"' app/api/dv/summarize/route.ts` returns 1 (model identity locked per CONTEXT D-14)
    - `grep -c 'signal: req.signal' app/api/dv/summarize/route.ts` returns 1 (AI-07 signal propagation)
    - `grep -c '"dv-summarize"' app/api/dv/summarize/route.ts` returns 1 (rate-limit key)
    - `grep -c 'max: 10' app/api/dv/summarize/route.ts` returns 1 (10/min CONTEXT D-13)
    - `grep -c 'summary_ai' app/api/dv/summarize/route.ts` returns ≥3 (read for cache + update column + freshness column)
    - `grep -c '"X-Source": "cache"' app/api/dv/summarize/route.ts` returns 1
    - `grep -c '"X-Source": "fresh"' app/api/dv/summarize/route.ts` returns 1
    - `grep -c '"Cache-Control": "no-store"' app/api/dv/summarize/route.ts` returns 2 (both cache-hit and cache-miss paths)
    - `grep -c "finally" app/api/dv/summarize/route.ts` returns 0 (CRITICAL — no finally block per RESEARCH Q6)
    - `awk '/start\(controller\)/,/^[[:space:]]*\},$/' app/api/dv/summarize/route.ts | grep -c "supabase.*update"` returns 1 (write-back appears exactly once, inside the start() callback)
    - `bunx tsc --noEmit` exits 0
    - `bun run build` exits 0; `/api/dv/summarize` listed in the build output as a dynamic route
  </acceptance_criteria>
  <verify>
    <automated>bunx tsc --noEmit && bun run build && grep -c "finally" app/api/dv/summarize/route.ts</automated>
    <automated_note>The "no finally" grep is critical — RESEARCH Q6 explicitly forbids finally blocks because they run on abort and would poison the cache with partial summary text. The grep returns 0 (no finally keyword anywhere in the file).</automated_note>
  </verify>
</task>

<task type="auto">
  <name>Task 2: __tests__/dv-summarize-route.test.ts (rate-limit + model identity + signal propagation + cache hit/miss + abort no-poison)</name>
  <files>/Users/beyond/Desktop/lex-web/__tests__/dv-summarize-route.test.ts</files>
  <read_first>
    - /Users/beyond/Desktop/lex-web/app/api/dv/summarize/route.ts (Task 1)
    - /Users/beyond/Desktop/lex-web/.planning/phases/08-dv-gazette/08-VALIDATION.md row 08-03-01 (acceptance criteria)
    - /Users/beyond/Desktop/lex-web/lib/rate-limit.ts (Phase 1 server gate — mock or import?)
  </read_first>
  <action>
    Create `/Users/beyond/Desktop/lex-web/__tests__/dv-summarize-route.test.ts` with 6 cases:

    1. **rate-limit 429** — invoke POST 11 times with same X-Forwarded-For; 11th returns 429
    2. **model identity grep gate** — read route.ts source; assert `claude-sonnet-4-6` appears, `claude-sonnet-4-5` does NOT appear (no bleed-through from older sonnet versions)
    3. **signal propagation grep gate** — read route.ts source; assert `signal: req.signal` appears exactly once
    4. **cache hit short-circuit** — mock Supabase to return a row with `summary_ai="cached body"`; assert response body equals "cached body", `X-Source: cache` header, NO Anthropic call made
    5. **cache miss write-back** — mock Supabase to return a row with `summary_ai=null`, mock Anthropic to stream "fresh summary"; assert response streams "fresh summary", AND after completion, Supabase `.update()` is called with the collected text
    6. **abort no-poison** — mock Anthropic stream to throw mid-stream (simulating abort); assert Supabase `.update()` is NOT called; partial collected text is NOT persisted

    The tests mock both `@anthropic-ai/sdk` and `@supabase/supabase-js` at the module level. Use `vi.mock` to intercept the module imports.

    Full source:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { readFileSync } from "node:fs";

    // Mock Anthropic SDK
    const mockStream = vi.fn();
    vi.mock("@anthropic-ai/sdk", () => {
      return {
        default: class MockAnthropic {
          messages = {
            stream: mockStream,
          };
        },
      };
    });

    // Mock Supabase
    const mockSelect = vi.fn();
    const mockUpdate = vi.fn();
    const mockCreateClient = vi.fn(() => ({
      from: () => ({
        select: () => ({
          eq: () => ({
            limit: () => ({
              single: mockSelect,
            }),
          }),
        }),
        update: (data: any) => ({
          eq: (col: string, val: any) => {
            mockUpdate({ data, col, val });
            return { error: null };
          },
        }),
      }),
    }));

    vi.mock("@supabase/supabase-js", () => ({
      createClient: mockCreateClient,
    }));

    // Mock rate-limit so we control which calls return 429
    const mockRateLimited = vi.fn();
    vi.mock("@/lib/rate-limit", () => ({
      rateLimited: mockRateLimited,
    }));

    // Set required env vars for service client
    process.env.NEXT_PUBLIC_SUPABASE_URL = "https://test.supabase.co";
    process.env.SUPABASE_SERVICE_ROLE_KEY = "test-service-key";
    process.env.AUDIT_VOTE_SALT = "test-salt";
    process.env.ANTHROPIC_API_KEY = "test-anthropic-key";

    // Helper: build a Request with the standard shape
    function mkRequest(body: object, signal?: AbortSignal): Request {
      return new Request("https://example.com/api/dv/summarize", {
        method: "POST",
        headers: { "Content-Type": "application/json", "x-forwarded-for": "1.2.3.4" },
        body: JSON.stringify(body),
        signal,
      });
    }

    // Helper: drain a ReadableStream body to a string
    async function drainBody(response: Response): Promise<string> {
      const reader = response.body?.getReader();
      if (!reader) return "";
      const decoder = new TextDecoder();
      let result = "";
      while (true) {
        const { value, done } = await reader.read();
        if (done) break;
        result += decoder.decode(value, { stream: true });
      }
      result += decoder.decode();
      return result;
    }

    describe("/api/dv/summarize — grep gates", () => {
      it("uses claude-sonnet-4-6 model (no version bleed-through)", () => {
        const source = readFileSync(`${process.cwd()}/app/api/dv/summarize/route.ts`, "utf-8");
        expect(source).toMatch(/"claude-sonnet-4-6"/);
        expect(source).not.toMatch(/claude-sonnet-4-5(?!\.\d)/); // ensure no 4-5 leakage
      });

      it("forwards signal: req.signal to Anthropic stream (AI-07 preservation)", () => {
        const source = readFileSync(`${process.cwd()}/app/api/dv/summarize/route.ts`, "utf-8");
        expect(source).toMatch(/signal:\s*req\.signal/);
      });

      it("contains zero `finally` blocks (cache-poison prevention per RESEARCH Q6)", () => {
        const source = readFileSync(`${process.cwd()}/app/api/dv/summarize/route.ts`, "utf-8");
        const finallyMatches = source.match(/\bfinally\b/g);
        expect(finallyMatches).toBeNull();
      });

      it("uses dv-summarize rate-limit key with max=10/min", () => {
        const source = readFileSync(`${process.cwd()}/app/api/dv/summarize/route.ts`, "utf-8");
        expect(source).toMatch(/"dv-summarize"/);
        expect(source).toMatch(/max:\s*10/);
        expect(source).toMatch(/windowMs:\s*60_000/);
      });
    });

    describe("/api/dv/summarize — behavior", () => {
      beforeEach(() => {
        mockSelect.mockReset();
        mockUpdate.mockReset();
        mockStream.mockReset();
        mockRateLimited.mockReset();
        mockRateLimited.mockReturnValue(null); // default: not rate-limited
      });

      it("returns 429 when rate-limited", async () => {
        // Override rate-limit to return a 429 response
        mockRateLimited.mockReturnValueOnce(
          new Response("Превишен лимит", { status: 429, headers: { "Retry-After": "60" } }),
        );
        const { POST } = await import("@/app/api/dv/summarize/route");
        const res = await POST(mkRequest({ actId: "uuid-1" }));
        expect(res.status).toBe(429);
      });

      it("cache hit: returns cached summary_ai with X-Source: cache; NO Anthropic call", async () => {
        mockSelect.mockResolvedValueOnce({
          data: { id: "uuid-1", title: "Test", act_type: "Указ", full_text: "...", summary_ai: "cached body" },
          error: null,
        });

        const { POST } = await import("@/app/api/dv/summarize/route");
        const res = await POST(mkRequest({ actId: "uuid-1" }));

        expect(res.status).toBe(200);
        expect(res.headers.get("X-Source")).toBe("cache");
        expect(res.headers.get("Content-Type")).toBe("text/plain; charset=utf-8");
        expect(res.headers.get("Cache-Control")).toBe("no-store");
        const body = await drainBody(res);
        expect(body).toBe("cached body");
        expect(mockStream).not.toHaveBeenCalled();
        expect(mockUpdate).not.toHaveBeenCalled();
      });

      it("cache miss: streams Anthropic + write-back after complete", async () => {
        mockSelect.mockResolvedValueOnce({
          data: { id: "uuid-1", title: "Test", act_type: "Закон", full_text: "Long act body".repeat(100), summary_ai: null },
          error: null,
        });

        // Mock the Anthropic stream to yield 3 chunks
        const fakeEvents = [
          { type: "content_block_delta", delta: { type: "text_delta", text: "## Какво прави\n" } },
          { type: "content_block_delta", delta: { type: "text_delta", text: "Тестово обобщение. " } },
          { type: "content_block_delta", delta: { type: "text_delta", text: "Край." } },
        ];
        mockStream.mockResolvedValueOnce({
          [Symbol.asyncIterator]: async function* () {
            for (const e of fakeEvents) yield e;
          },
        });

        const { POST } = await import("@/app/api/dv/summarize/route");
        const res = await POST(mkRequest({ actId: "uuid-1" }));

        expect(res.status).toBe(200);
        expect(res.headers.get("X-Source")).toBe("fresh");
        const body = await drainBody(res);
        expect(body).toBe("## Какво прави\nТестово обобщение. Край.");

        // After stream drain, write-back should have been called
        expect(mockUpdate).toHaveBeenCalledTimes(1);
        expect(mockUpdate.mock.calls[0][0].data.summary_ai).toBe("## Какво прави\nТестово обобщение. Край.");
        expect(mockUpdate.mock.calls[0][0].data.summary_ai_generated_at).toMatch(/^\d{4}-\d{2}-\d{2}T/);
      });

      it("abort mid-stream: NO write-back (cache-poison prevention)", async () => {
        mockSelect.mockResolvedValueOnce({
          data: { id: "uuid-1", title: "Test", act_type: "Закон", full_text: "Long act body".repeat(100), summary_ai: null },
          error: null,
        });

        // Stream yields one chunk then throws
        mockStream.mockResolvedValueOnce({
          [Symbol.asyncIterator]: async function* () {
            yield { type: "content_block_delta", delta: { type: "text_delta", text: "partial " } };
            throw new Error("abort");
          },
        });

        const { POST } = await import("@/app/api/dv/summarize/route");
        const res = await POST(mkRequest({ actId: "uuid-1" }));

        // Response is still 200 (stream starts before the error); the error happens during drain
        try {
          await drainBody(res);
        } catch {
          // Expected — stream errored
        }
        // Write-back MUST NOT have been called
        expect(mockUpdate).not.toHaveBeenCalled();
      });

      it("missing actId returns 400", async () => {
        const { POST } = await import("@/app/api/dv/summarize/route");
        const res = await POST(mkRequest({}));
        expect(res.status).toBe(400);
      });

      it("act not found returns 404", async () => {
        mockSelect.mockResolvedValueOnce({ data: null, error: { message: "not found" } });
        const { POST } = await import("@/app/api/dv/summarize/route");
        const res = await POST(mkRequest({ actId: "uuid-missing" }));
        expect(res.status).toBe(404);
      });

      it("empty full_text returns 422", async () => {
        mockSelect.mockResolvedValueOnce({
          data: { id: "uuid-1", title: "Test", act_type: "Указ", full_text: "", summary_ai: null },
          error: null,
        });
        const { POST } = await import("@/app/api/dv/summarize/route");
        const res = await POST(mkRequest({ actId: "uuid-1" }));
        expect(res.status).toBe(422);
      });
    });
    ```

    Run `bun run test __tests__/dv-summarize-route.test.ts` and verify all cases pass.
  </action>
  <acceptance_criteria>
    - File `__tests__/dv-summarize-route.test.ts` exists
    - `bun run test __tests__/dv-summarize-route.test.ts` exits 0 with all 11 cases passing (4 grep-gate + 7 behavior)
    - Test count covers (a) rate-limit 429, (b) cache hit short-circuit, (c) cache miss write-back, (d) abort no-poison, (e) missing actId 400, (f) act not found 404, (g) empty full_text 422, PLUS 4 source-grep gates (model identity, signal propagation, no-finally, rate-limit key+max+window)
    - `bunx tsc --noEmit` exits 0
    - Plan-wide: `bun run test` exits 0 (full suite green); `bun run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bun run test __tests__/dv-summarize-route.test.ts && bunx tsc --noEmit && bun run build</automated>
  </verify>
</task>

</tasks>

<verification>

After both tasks complete, the executor runs:

```bash
# 1. Plan-specific test green
bun run test __tests__/dv-summarize-route.test.ts
# Expect: 11 cases passing

# 2. Full suite remains green (no regression in Phase 1 or 08-02 tests)
bun run test
# Expect: all cases passing

# 3. Type check + build
bunx tsc --noEmit && bun run build
# Expect: both exit 0; /api/dv/summarize registered as ƒ (Dynamic) route

# 4. Critical "no finally" grep gate
grep -c "finally" app/api/dv/summarize/route.ts
# Expect: 0

# 5. Critical signal-propagation grep gate
grep -c "signal: req.signal" app/api/dv/summarize/route.ts
# Expect: 1

# 6. Critical model-identity grep gate
grep -c "claude-sonnet-4-6" app/api/dv/summarize/route.ts
# Expect: 1
```

All checks must pass.

</verification>

<must_haves>

Goal-backward verification: these are the things that MUST be true for the DV-02 AI surface to be considered satisfied at the end of plan 08-03:

- POST /api/dv/summarize is a working server endpoint reachable from /dv/[slug] client components (08-02's DvActSummary calls it)
- Sonnet 4.6 is the model in use (CONTEXT D-14)
- Rate-limit `dv-summarize` 10/min/IP enforced (CONTEXT D-13)
- `signal: req.signal` forwarded to Anthropic stream (AI-07 preservation from Phase 1/2)
- Cache hit returns the cached `summary_ai` text instantly with `X-Source: cache` header
- Cache miss streams a fresh response AND writes back to `dv_acts.summary_ai` ONLY on clean completion (NEVER on abort)
- Zero `finally` blocks (RESEARCH Q6 invariant — finally would poison cache)
- All 11 test cases pass; full suite green; TypeScript clean; build succeeds

</must_haves>

<threat_model>

**T-DV-03-01 Cache poisoning via partial-stream write-back (Tampering — most critical)**
- Threat: AbortSignal-triggered upstream cancellation in the middle of a stream leaves `collected` truncated; if the route writes-back regardless, future readers see a half-sentence cached forever
- Mitigation: Write-back lives INSIDE the `try` block AFTER the `for await` loop. On any exception or abort, the catch block runs `controller.error(err)` and EXITS without calling `.update()`. RESEARCH Q6 explicitly documents this. Grep gate `grep -c "finally" route.ts → 0` enforces no `finally` block exists.
- Severity: HIGH if implemented wrong; mitigated by code structure + grep gate + dedicated test case (Task 2 case 4)

**T-DV-03-02 Token-budget DoS (DoS)**
- Threat: malicious actor floods /api/dv/summarize for arbitrary actId values to burn through Anthropic budget
- Mitigation: rateLimited("dv-summarize", windowMs=60_000, max=10) per Phase 1 in-memory limiter (D-09/D-10); HMAC ip_hash logged for audit. Cache write-back means each unique actId costs Anthropic ONCE across all readers (subsequent calls hit cache).
- Severity: medium (10/min/IP at $0.015 avg/call = $9/hr/IP attacker; bounded by Phase 1 limiter)

**T-DV-03-03 IDOR via guessed actId (Information disclosure)**
- Threat: attacker enumerates dv_acts.id UUIDs to generate summaries for acts they shouldn't see
- Mitigation: dv_acts is PUBLIC data (Bulgarian gazette is openly published). Enumeration is not a violation. UUID v4 IDs are unguessable but not relied on for security.
- Severity: none (public-data corpus)

**T-DV-03-04 SSRF via Anthropic SDK abuse (Tampering)**
- Threat: an attacker controls input that crafts an Anthropic request which somehow exfiltrates internal data
- Mitigation: All user input is forwarded ONLY as `messages[].content` text to Anthropic; no URL fetching, no tool use, no file access. The Anthropic API is a closed system from this endpoint's perspective.
- Severity: low (no SSRF vector)

**T-DV-03-05 PII / sensitive data exfiltration through Anthropic (Information disclosure)**
- Threat: act bodies may contain personal names of citizens or officials; sending to Anthropic without redaction "leaks" them to a third party
- Mitigation: Bulgarian State Gazette publishes these acts PUBLICLY. Sending public data to Anthropic for summarization is the explicit user-requested feature, not a leak. Anthropic's data-use policy applies (no training on customer data per their API terms).
- Severity: none (data is already public)

**T-DV-03-06 Service-role key leak (Spoofing / Elevation of Privilege)**
- Threat: `SUPABASE_SERVICE_ROLE_KEY` env var leaks into client bundle or logs
- Mitigation: route handler runs server-side only (runtime: "nodejs"); `getServiceSupabase()` reads `process.env` server-side; no `NEXT_PUBLIC_` prefix on the service role key (Next.js convention prevents bundling); env var is set on Vercel only, not committed to repo.
- Severity: low (architectural barrier; Vercel env-var hygiene)

</threat_model>
