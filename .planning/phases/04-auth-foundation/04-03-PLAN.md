---
phase: 04
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - /Users/beyond/Desktop/lex-web/app/sign-in/page.tsx
  - /Users/beyond/Desktop/lex-web/app/sign-in/sign-in-form.tsx
  - /Users/beyond/Desktop/lex-web/app/sign-up/page.tsx
  - /Users/beyond/Desktop/lex-web/app/sign-up/sign-up-form.tsx
  - /Users/beyond/Desktop/lex-web/app/sign-up/check-email/page.tsx
  - /Users/beyond/Desktop/lex-web/app/auth-nav-link.tsx
  - /Users/beyond/Desktop/lex-web/app/layout.tsx
  - /Users/beyond/Desktop/lex-web/__tests__/sign-in-form.test.tsx
  - /Users/beyond/Desktop/lex-web/__tests__/sign-up-form.test.tsx
  - /Users/beyond/Desktop/lex-web/__tests__/auth-nav-link.test.tsx
task_count: 3
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-04]
tags: [auth, ui, forms, navbar, bulgarian, oauth, manual-smoke]
must_haves:
  truths:
    - "User can navigate to `/sign-up`, fill in email + password + display name, submit, and the form calls `supabase.auth.signUp` with `email`, `password`, `options.data.display_name`, `options.emailRedirectTo` (D-03)."
    - "User submitting valid sign-up is routed to `/sign-up/check-email` (D-02 — magic-link verification, no OTP UI)."
    - "User can navigate to `/sign-in`, fill in email + password, submit, and the form calls `supabase.auth.signInWithPassword` and on success routes to `/`."
    - "User can click 'Влез с Google' on `/sign-in`, and the form calls `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: …/auth/callback?next=/ } })`."
    - "Sign-up form rejects empty/whitespace-only display name (D-03 — required field)."
    - "Bulgarian error messages are displayed for the standard error families (D-10 — formal-legal tone)."
    - "Navbar in `app/layout.tsx` renders an `<AuthNavLink>` client component, right-aligned with `hover:underline underline-offset-4` to match `/audit` and `/intel` pattern (D-09). Anonymous users see 'Влез' linking to `/sign-in`. Signed-in users see 'Профил' linking to `/account` (Phase 6 will populate the page; for Phase 4 it can be a placeholder route → fine because Phase 5 middleware doesn't ship yet)."
    - "Cross-links between pages exist: `/sign-up` has 'Имаш профил? Влез' → `/sign-in`; `/sign-in` has 'Нямаш профил? Регистрирай се' → `/sign-up` (D-04)."
    - "All three new vitest test files pass."
    - "BLOCKING manual smoke confirms: real Google OAuth consent flow → callback → user_profiles row → navbar shows 'Профил'. Real email signup → Bulgarian email arrives → click → callback → navbar shows 'Профил'. Sign-out → navbar reverts to 'Влез'."
  artifacts:
    - path: app/sign-in/page.tsx
      provides: "Server Component shell rendering <SignInForm />"
      min_lines: 15
    - path: app/sign-in/sign-in-form.tsx
      provides: "'use client' hand-rolled Bulgarian sign-in form (email/password + Google OAuth button)"
      exports: ["SignInForm"]
      min_lines: 100
    - path: app/sign-up/page.tsx
      provides: "Server Component shell rendering <SignUpForm />"
      min_lines: 15
    - path: app/sign-up/sign-up-form.tsx
      provides: "'use client' hand-rolled Bulgarian sign-up form (email/password/display-name)"
      exports: ["SignUpForm"]
      min_lines: 100
    - path: app/sign-up/check-email/page.tsx
      provides: "Bulgarian 'check your email' confirmation page (D-02)"
      min_lines: 15
    - path: app/auth-nav-link.tsx
      provides: "'use client' navbar link — Влез/Профил via useSession()"
      exports: ["AuthNavLink"]
      min_lines: 25
    - path: app/layout.tsx
      provides: "MODIFIED — adds <AuthNavLink /> at end of nav, right-aligned"
      contains: "AuthNavLink"
    - path: __tests__/sign-in-form.test.tsx
      provides: "Tests: signInWithPassword shape, signInWithOAuth shape (Google + redirectTo), error mapping"
      min_lines: 80
    - path: __tests__/sign-up-form.test.tsx
      provides: "Tests: signUp shape (email, password, options.data.display_name, options.emailRedirectTo), required-field validation, duplicate-email error mapping, router.push to /sign-up/check-email"
      min_lines: 80
    - path: __tests__/auth-nav-link.test.tsx
      provides: "Tests: anonymous → 'Влез' link to /sign-in; signed-in → 'Профил' link to /account"
      min_lines: 40
  key_links:
    - from: "app/sign-up/sign-up-form.tsx"
      to: "supabase.auth.signUp"
      via: "createBrowserSupabase().auth.signUp({ email, password, options: { emailRedirectTo, data: { display_name } } })"
      pattern: "supabase\\.auth\\.signUp"
    - from: "app/sign-up/sign-up-form.tsx"
      to: "router.push to /sign-up/check-email"
      via: "next/navigation useRouter"
      pattern: 'router\.push\("/sign-up/check-email"\)'
    - from: "app/sign-in/sign-in-form.tsx"
      to: "supabase.auth.signInWithPassword"
      via: "browser-side call"
      pattern: "signInWithPassword"
    - from: "app/sign-in/sign-in-form.tsx"
      to: "supabase.auth.signInWithOAuth"
      via: "Google provider with redirectTo /auth/callback?next=/"
      pattern: 'signInWithOAuth.*provider.*google'
    - from: "app/layout.tsx"
      to: "app/auth-nav-link.tsx"
      via: "import { AuthNavLink } from '@/app/auth-nav-link'"
      pattern: "AuthNavLink"
    - from: "app/auth-nav-link.tsx"
      to: "lib/use-session.ts"
      via: "import { useSession }"
      pattern: '@/lib/use-session'
success_criteria:
  - "All 6 new source files exist + the modified `app/layout.tsx` imports + renders `<AuthNavLink />`."
  - "`grep -c 'supabase.auth.signUp' app/sign-up/sign-up-form.tsx` returns 1."
  - "`grep -c 'options.data.display_name\\|display_name:' app/sign-up/sign-up-form.tsx` returns ≥1."
  - "`grep -c 'emailRedirectTo' app/sign-up/sign-up-form.tsx` returns 1."
  - "`grep -c 'signInWithOAuth' app/sign-in/sign-in-form.tsx` returns 1."
  - "`grep -c \"provider: 'google'\\|provider: \\\"google\\\"\" app/sign-in/sign-in-form.tsx` returns 1."
  - "`grep -q 'AuthNavLink' app/layout.tsx`."
  - "`grep -q 'hover:underline underline-offset-4' app/auth-nav-link.tsx` (D-09 navbar pattern)."
  - "`grep -qE 'Влез|Влезте|Регистрирай' app/sign-in/sign-in-form.tsx app/sign-up/sign-up-form.tsx` (D-10 Bulgarian copy present)."
  - "All 3 new vitest test files pass: `bun run test -- __tests__/sign-in-form.test.tsx __tests__/sign-up-form.test.tsx __tests__/auth-nav-link.test.tsx` exits 0."
  - "Full vitest suite green: `bun run test` exits 0 (no Phase 1/2/8 regressions)."
  - "`npx tsc --noEmit` exits 0."
  - "`bun run build` exits 0."
  - "BLOCKING manual smoke: operator completes the 4 verifications below (Google OAuth, email signup, sign-out, anonymous /audit/vote unchanged)."
---

<objective>
Ship the user-facing surface of Phase 4: hand-rolled Bulgarian `/sign-in`, `/sign-up`, and `/sign-up/check-email` pages (D-01, D-02, D-04, D-10), styled to match the existing `/audit` + `/intel` design tokens (red accents, font-serif headings, stone-900 surfaces). Add a tiny `<AuthNavLink>` client component to the navbar in `app/layout.tsx` that flips between "Влез" (anonymous) and "Профил" (signed-in) reactively via the `useSession()` hook from Plan 04-02 (D-09 — right-aligned, `hover:underline underline-offset-4` to match the existing nav).

Email signup metadata carries `display_name` (D-03) which the trigger from Plan 04-01 reads. The "check your email" page (D-02) is the magic-link landing — no OTP UI to build.

Three new vitest files cover the unit-testable form surface: `signUp`/`signInWithPassword`/`signInWithOAuth` invocation shapes, required-field validation, Bulgarian error mapping, and the navbar Влез↔Профил toggle. The actual Google OAuth consent screen + real email-link flow are NOT vitest-able; they're a **BLOCKING manual smoke** at the end of Task 3 (mirrors the Phase 8 `08-01` live-net smoke pattern).

Closes the *UI* half of AUTH-01 + AUTH-02, and the *navbar* half of AUTH-04 (the server util + client hook landed in Plan 04-02). At plan completion, all four AUTH requirements are satisfied.

Output: 7 new source files (3 form/page + 3 server-shell pages reusing form components + 1 nav-link client component), 1 modified file (layout.tsx), 3 new test files, BLOCKING smoke completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beyond/Desktop/lex-web/.planning/phases/04-auth-foundation/04-CONTEXT.md
@/Users/beyond/Desktop/lex-web/.planning/phases/04-auth-foundation/04-RESEARCH.md
@/Users/beyond/Desktop/lex-web/app/layout.tsx
@/Users/beyond/Desktop/lex-web/app/laws/[slug]/alert-form.tsx
@/Users/beyond/Desktop/lex-web/lib/supabase-auth.ts
@/Users/beyond/Desktop/lex-web/lib/use-session.ts

<interfaces>
<!-- Existing form pattern (app/laws/[slug]/alert-form.tsx — REFERENCE — DO NOT MODIFY): -->
<!--   "use client"; -->
<!--   import { useState } from "react"; -->
<!--   type Status = "idle" | "submitting" | "ok" | "error"; -->
<!--   const [email, setEmail] = useState(""); -->
<!--   const [status, setStatus] = useState<Status>("idle"); -->
<!--   const [message, setMessage] = useState<string | null>(null); -->
<!--   onSubmit: e.preventDefault(); setStatus("submitting"); …; setStatus("ok"|"error") -->
<!--   New forms in this plan should follow this exact shape (vs. react-hook-form / zod — RESEARCH §"NOT added"). -->
<!-- -->
<!-- From lib/supabase-auth.ts (Plan 04-02 — already shipped): -->
<!--   export function createBrowserSupabase(): SupabaseClient; -->
<!--   export async function getSession(): Promise<User | null>; -->
<!-- -->
<!-- From lib/use-session.ts (Plan 04-02 — already shipped): -->
<!--   export function useSession(): { user: User | null; loading: boolean }; -->
<!-- -->
<!-- From next/navigation: -->
<!--   import { useRouter } from "next/navigation"; -->
<!--   const router = useRouter(); -->
<!--   router.push("/path") -->
<!-- -->
<!-- From next/link: -->
<!--   import Link from "next/link"; -->
<!--   <Link href="…" className="…">…</Link> -->
<!-- -->
<!-- Design-token reference (verbatim from /audit + /intel patterns surfaced in app/layout.tsx and existing pages): -->
<!--   - red accent: text-red-700 dark:text-red-400 -->
<!--   - serif heading: font-serif font-semibold tracking-tight -->
<!--   - link hover: hover:underline underline-offset-4 -->
<!--   - card surface: bg-stone-50 border border-black/[0.08] dark:border-white/[0.08] dark:bg-stone-900 (or similar — match existing /audit cards) -->
<!--   - input focus: focus:outline-none focus:ring-2 focus:ring-red-500 -->
<!--   - button primary: bg-red-700 text-white hover:bg-red-800 -->
<!-- -->
<!-- Bulgarian copy bank (D-10 — formal-legal): -->
<!--   - "Влезте в профила си" (sign-in heading) -->
<!--   - "Регистрирайте се" (sign-up heading) -->
<!--   - Field labels: "Имейл", "Парола", "Име" (display name) -->
<!--   - Buttons: "Влез", "Регистрирай се", "Влез с Google" -->
<!--   - Errors: "Невалидна парола" / "Този имейл е вече регистриран." / "Невалидни данни за вход." / "Грешка при регистрация. Моля, опитайте отново." -->
<!--   - Cross-link: sign-in → "Нямаш профил? Регистрирай се"; sign-up → "Имаш профил? Влез" -->
<!--   - Check-email: "Изпратихме потвърждение на имейла Ви" + "Натиснете върху линка в имейла, за да завършите регистрацията." -->
<!--   - Navbar: anonymous = "Влез"; signed-in = "Профил" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sign-up surface — page, hand-rolled Bulgarian form, check-email page, vitest</name>
  <files>/Users/beyond/Desktop/lex-web/app/sign-up/page.tsx, /Users/beyond/Desktop/lex-web/app/sign-up/sign-up-form.tsx, /Users/beyond/Desktop/lex-web/app/sign-up/check-email/page.tsx, /Users/beyond/Desktop/lex-web/__tests__/sign-up-form.test.tsx</files>
  <behavior>
- `<SignUpForm>` collects three fields: email, password, display name. All required, trimmed.
- On submit: calls `supabase.auth.signUp({ email, password, options: { emailRedirectTo: '${origin}/auth/callback?next=/', data: { display_name } } })`.
- On success: routes to `/sign-up/check-email` via `useRouter().push`.
- On error: maps known Supabase error message families to formal-legal Bulgarian (D-10).
- Disables submit while submitting and when any field is empty/whitespace.
- The page is a Server Component shell that imports + renders `<SignUpForm />` with the heading "Регистрирайте се" and the cross-link "Имаш профил? Влез" → `/sign-in` (D-04).
- The check-email page is a static Server Component with a confirmation message (D-02).
- Tests assert the signUp call shape, the router redirect, the error-mapping, and the required-field validation.
  </behavior>
  <action>

### Step 1.1 — Write `app/sign-up/sign-up-form.tsx` (hand-rolled per D-01, Bulgarian per D-10)

```tsx
// File: app/sign-up/sign-up-form.tsx
//
// Hand-rolled Bulgarian sign-up form (D-01, D-10).
// Uses the alert-form.tsx state-machine pattern: useState + onSubmit + Status enum.
// Magic-link verification (D-02): success routes to /sign-up/check-email.
// display_name (D-03) flows into raw_user_meta_data, read by the handle_new_user
// trigger from Plan 04-01.

"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { createBrowserSupabase } from "@/lib/supabase-auth";

type Status = "idle" | "submitting" | "ok" | "error";

export function SignUpForm() {
  const router = useRouter();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [displayName, setDisplayName] = useState("");
  const [status, setStatus] = useState<Status>("idle");
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  const canSubmit =
    email.trim().length > 0 &&
    password.length > 0 &&
    displayName.trim().length > 0 &&
    status !== "submitting";

  async function onSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (!canSubmit) return;
    setStatus("submitting");
    setErrorMessage(null);

    const supabase = createBrowserSupabase();
    const { error } = await supabase.auth.signUp({
      email: email.trim(),
      password,
      options: {
        // Magic link lands on /auth/callback?code=xxx — exchanges for session,
        // then redirects to `next` (default /). RESEARCH §Pattern 5.
        emailRedirectTo: `${window.location.origin}/auth/callback?next=/`,
        data: {
          // Read by handle_new_user() trigger (D-03, D-05).
          display_name: displayName.trim(),
        },
      },
    });

    if (error) {
      setStatus("error");
      setErrorMessage(translateAuthError(error.message));
      return;
    }

    setStatus("ok");
    router.push("/sign-up/check-email");
  }

  return (
    <form
      onSubmit={onSubmit}
      className="w-full max-w-md space-y-4 rounded-md border border-black/[0.08] dark:border-white/[0.08] bg-stone-50 dark:bg-stone-900 p-6"
    >
      <div className="space-y-1">
        <label htmlFor="signup-email" className="block text-sm font-medium">
          Имейл
        </label>
        <input
          id="signup-email"
          type="email"
          autoComplete="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          required
          className="w-full rounded-md border border-black/[0.12] dark:border-white/[0.12] bg-white dark:bg-stone-800 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500"
        />
      </div>

      <div className="space-y-1">
        <label htmlFor="signup-password" className="block text-sm font-medium">
          Парола
        </label>
        <input
          id="signup-password"
          type="password"
          autoComplete="new-password"
          minLength={6}
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          required
          className="w-full rounded-md border border-black/[0.12] dark:border-white/[0.12] bg-white dark:bg-stone-800 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500"
        />
      </div>

      <div className="space-y-1">
        <label htmlFor="signup-display-name" className="block text-sm font-medium">
          Име
        </label>
        <input
          id="signup-display-name"
          type="text"
          autoComplete="name"
          value={displayName}
          onChange={(e) => setDisplayName(e.target.value)}
          required
          className="w-full rounded-md border border-black/[0.12] dark:border-white/[0.12] bg-white dark:bg-stone-800 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500"
        />
      </div>

      {status === "error" && errorMessage ? (
        <p
          role="alert"
          className="text-sm text-red-700 dark:text-red-400"
        >
          {errorMessage}
        </p>
      ) : null}

      <button
        type="submit"
        disabled={!canSubmit}
        className="w-full rounded-md bg-red-700 px-4 py-2 text-sm font-medium text-white hover:bg-red-800 disabled:cursor-not-allowed disabled:opacity-50"
      >
        {status === "submitting" ? "Изпращане…" : "Регистрирай се"}
      </button>

      <p className="text-center text-sm text-black/65 dark:text-white/65">
        Имаш профил?{" "}
        <Link href="/sign-in" className="hover:underline underline-offset-4 text-red-700 dark:text-red-400">
          Влез
        </Link>
      </p>
    </form>
  );
}

function translateAuthError(msg: string): string {
  // Map common Supabase Auth error families to formal-legal Bulgarian (D-10).
  if (/already registered/i.test(msg)) return "Този имейл е вече регистриран.";
  if (/Password/i.test(msg)) return "Невалидна парола (минимум 6 символа).";
  if (/Email/i.test(msg)) return "Невалиден имейл адрес.";
  return "Грешка при регистрация. Моля, опитайте отново.";
}
```

### Step 1.2 — Write `app/sign-up/page.tsx`

```tsx
// File: app/sign-up/page.tsx
//
// Server Component shell. Renders the hand-rolled <SignUpForm /> in the
// site's standard centered card layout. Bulgarian heading per D-10.

import type { Metadata } from "next";
import { SignUpForm } from "./sign-up-form";

export const metadata: Metadata = {
  title: "Регистрация",
  description: "Регистрирайте се в lex.bg, за да получите достъп до AI инструменти за анализ на българското законодателство.",
};

export default function SignUpPage() {
  return (
    <div className="mx-auto max-w-md px-4 py-12 sm:px-6">
      <h1 className="font-serif text-2xl font-semibold tracking-tight text-center mb-6">
        Регистрирайте се
      </h1>
      <SignUpForm />
    </div>
  );
}
```

### Step 1.3 — Write `app/sign-up/check-email/page.tsx`

```tsx
// File: app/sign-up/check-email/page.tsx
//
// Magic-link landing — D-02 (no OTP UI). User arrives here after a successful
// supabase.auth.signUp(). Reads the email, clicks the link, lands on
// /auth/callback which exchanges the code → session cookie → home.

import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Потвърдете имейла си",
  description: "Изпратихме потвърждение на имейла Ви. Натиснете върху линка в имейла, за да завършите регистрацията.",
};

export default function CheckEmailPage() {
  return (
    <div className="mx-auto max-w-md px-4 py-12 sm:px-6 text-center">
      <h1 className="font-serif text-2xl font-semibold tracking-tight mb-4">
        Изпратихме потвърждение на имейла Ви
      </h1>
      <p className="text-sm text-black/75 dark:text-white/75 mb-6">
        Натиснете върху линка в имейла, за да завършите регистрацията.
        Ако не виждате имейла, проверете и спам папката.
      </p>
      <Link
        href="/sign-in"
        className="text-sm hover:underline underline-offset-4 text-red-700 dark:text-red-400"
      >
        Към вход
      </Link>
    </div>
  );
}
```

### Step 1.4 — Write `__tests__/sign-up-form.test.tsx`

```tsx
// File: __tests__/sign-up-form.test.tsx
//
// Asserts <SignUpForm /> behavior:
//  - signUp invoked with email, password, options.data.display_name, options.emailRedirectTo
//  - successful signUp routes to /sign-up/check-email
//  - duplicate-email error → Bulgarian message displayed
//  - submit disabled when any field is blank/whitespace

import { describe, expect, it, vi, beforeEach } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";

const signUpMock = vi.fn();
const pushMock = vi.fn();

vi.mock("@/lib/supabase-auth", () => ({
  createBrowserSupabase: () => ({
    auth: { signUp: signUpMock },
  }),
}));

vi.mock("next/navigation", () => ({
  useRouter: () => ({ push: pushMock }),
}));

import { SignUpForm } from "@/app/sign-up/sign-up-form";

describe("<SignUpForm>", () => {
  beforeEach(() => {
    signUpMock.mockReset();
    pushMock.mockReset();
    // jsdom needs window.location.origin for emailRedirectTo construction
    Object.defineProperty(window, "location", {
      value: { origin: "https://lex-web-eta.vercel.app" },
      configurable: true,
      writable: true,
    });
  });

  function fillFields({
    email = "test@example.bg",
    password = "supersecret",
    displayName = "Иван Петров",
  } = {}) {
    fireEvent.change(screen.getByLabelText(/имейл/i), { target: { value: email } });
    fireEvent.change(screen.getByLabelText(/парола/i), { target: { value: password } });
    fireEvent.change(screen.getByLabelText(/име/i), { target: { value: displayName } });
  }

  it("calls supabase.auth.signUp with email, password, display_name, emailRedirectTo", async () => {
    signUpMock.mockResolvedValueOnce({ data: { user: null }, error: null });

    render(<SignUpForm />);
    fillFields();
    fireEvent.click(screen.getByRole("button", { name: /регистрирай/i }));

    await waitFor(() => {
      expect(signUpMock).toHaveBeenCalledWith({
        email: "test@example.bg",
        password: "supersecret",
        options: {
          emailRedirectTo: "https://lex-web-eta.vercel.app/auth/callback?next=/",
          data: { display_name: "Иван Петров" },
        },
      });
    });
  });

  it("routes to /sign-up/check-email on success", async () => {
    signUpMock.mockResolvedValueOnce({ data: { user: null }, error: null });

    render(<SignUpForm />);
    fillFields();
    fireEvent.click(screen.getByRole("button", { name: /регистрирай/i }));

    await waitFor(() => {
      expect(pushMock).toHaveBeenCalledWith("/sign-up/check-email");
    });
  });

  it("shows Bulgarian error on duplicate email", async () => {
    signUpMock.mockResolvedValueOnce({
      data: null,
      error: { message: "User already registered" },
    });

    render(<SignUpForm />);
    fillFields();
    fireEvent.click(screen.getByRole("button", { name: /регистрирай/i }));

    expect(await screen.findByText(/вече регистриран/i)).toBeInTheDocument();
    expect(pushMock).not.toHaveBeenCalled();
  });

  it("disables submit when display name is whitespace-only (D-03 required field)", () => {
    render(<SignUpForm />);
    fireEvent.change(screen.getByLabelText(/имейл/i), { target: { value: "a@b.bg" } });
    fireEvent.change(screen.getByLabelText(/парола/i), { target: { value: "secret123" } });
    fireEvent.change(screen.getByLabelText(/име/i), { target: { value: "   " } });

    const button = screen.getByRole("button", { name: /регистрирай/i });
    expect(button).toBeDisabled();
  });

  it("trims email and display name before calling signUp", async () => {
    signUpMock.mockResolvedValueOnce({ data: { user: null }, error: null });

    render(<SignUpForm />);
    fillFields({ email: "  trim@me.bg  ", displayName: "  Иван  " });
    fireEvent.click(screen.getByRole("button", { name: /регистрирай/i }));

    await waitFor(() => {
      const call = signUpMock.mock.calls[0][0];
      expect(call.email).toBe("trim@me.bg");
      expect(call.options.data.display_name).toBe("Иван");
    });
  });
});
```

### Step 1.5 — Run the test

```bash
cd /Users/beyond/Desktop/lex-web
bun run test -- __tests__/sign-up-form.test.tsx
# Expect: 5 passes
```
  </action>
  <verify>
    <automated>cd /Users/beyond/Desktop/lex-web && test -f app/sign-up/page.tsx && test -f app/sign-up/sign-up-form.tsx && test -f app/sign-up/check-email/page.tsx && grep -q '"use client"' app/sign-up/sign-up-form.tsx && grep -q 'supabase.auth.signUp' app/sign-up/sign-up-form.tsx && grep -q 'display_name: displayName.trim()' app/sign-up/sign-up-form.tsx && grep -q 'emailRedirectTo' app/sign-up/sign-up-form.tsx && grep -q 'router.push("/sign-up/check-email")' app/sign-up/sign-up-form.tsx && grep -qE 'Регистрирайте се|Регистрирай се' app/sign-up/sign-up-form.tsx && grep -q 'Имейл' app/sign-up/sign-up-form.tsx && grep -q 'Имаш профил?' app/sign-up/sign-up-form.tsx && grep -q 'Изпратихме потвърждение' app/sign-up/check-email/page.tsx && bun run test -- __tests__/sign-up-form.test.tsx 2>&1 | tail -10 | grep -E 'pass|✓' && npx tsc --noEmit 2>&1 | (! grep -i "error TS")</automated>
  </verify>
  <done>
3 sign-up files + 1 test file shipped. signUp shape correct (email/password/options.data.display_name/emailRedirectTo). Routes to check-email on success. Bulgarian copy + cross-link to /sign-in present. 5/5 vitest cases pass; tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sign-in surface — page + form (email/password + Google OAuth) + AuthNavLink + layout integration + 2 vitest files</name>
  <files>/Users/beyond/Desktop/lex-web/app/sign-in/page.tsx, /Users/beyond/Desktop/lex-web/app/sign-in/sign-in-form.tsx, /Users/beyond/Desktop/lex-web/app/auth-nav-link.tsx, /Users/beyond/Desktop/lex-web/app/layout.tsx, /Users/beyond/Desktop/lex-web/__tests__/sign-in-form.test.tsx, /Users/beyond/Desktop/lex-web/__tests__/auth-nav-link.test.tsx</files>
  <behavior>
- `<SignInForm>`: email + password fields with submit → `supabase.auth.signInWithPassword({ email, password })`. On success: `router.push('/')`. On error: Bulgarian message.
- `<SignInForm>` also has a "Влез с Google" button that calls `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: '${origin}/auth/callback?next=/' } })` (no awaited handling — Supabase initiates the redirect).
- `<AuthNavLink>` is "use client", reads `useSession()`. Shows "Влез" → /sign-in (anonymous), or "Профил" → /account (signed-in). Returns null while `loading=true` to prevent SSR mismatch.
- `app/layout.tsx` MODIFIED: import `<AuthNavLink />` and append it as the LAST nav child, with the standard `hover:underline underline-offset-4` class (D-09). All other navbar links unchanged.
- Tests for sign-in form cover: signInWithPassword shape + router.push, signInWithOAuth shape (provider=google + redirectTo includes /auth/callback?next=/), error mapping for invalid credentials, cross-link to /sign-up.
- Tests for AuthNavLink: anonymous → "Влез" + href=/sign-in, signed-in → "Профил" + href=/account, loading → renders nothing.
  </behavior>
  <action>

### Step 2.1 — Write `app/sign-in/sign-in-form.tsx`

```tsx
// File: app/sign-in/sign-in-form.tsx
//
// Hand-rolled Bulgarian sign-in form (D-01, D-10) with email/password +
// Google OAuth button (D-02, AUTH-02).

"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { createBrowserSupabase } from "@/lib/supabase-auth";

type Status = "idle" | "submitting" | "ok" | "error";

export function SignInForm() {
  const router = useRouter();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [status, setStatus] = useState<Status>("idle");
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  const canSubmit =
    email.trim().length > 0 && password.length > 0 && status !== "submitting";

  async function onSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (!canSubmit) return;
    setStatus("submitting");
    setErrorMessage(null);

    const supabase = createBrowserSupabase();
    const { error } = await supabase.auth.signInWithPassword({
      email: email.trim(),
      password,
    });

    if (error) {
      setStatus("error");
      setErrorMessage(translateAuthError(error.message));
      return;
    }

    setStatus("ok");
    router.push("/");
  }

  async function onGoogleSignIn() {
    setStatus("submitting");
    setErrorMessage(null);

    const supabase = createBrowserSupabase();
    const { error } = await supabase.auth.signInWithOAuth({
      provider: "google",
      options: {
        // /auth/callback handles the PKCE code exchange — RESEARCH §Pattern 5.
        redirectTo: `${window.location.origin}/auth/callback?next=/`,
      },
    });

    if (error) {
      setStatus("error");
      setErrorMessage("Грешка при вход с Google. Моля, опитайте отново.");
    }
    // On success, Supabase initiates a redirect — no further code runs here.
  }

  return (
    <div className="w-full max-w-md space-y-4">
      <form
        onSubmit={onSubmit}
        className="space-y-4 rounded-md border border-black/[0.08] dark:border-white/[0.08] bg-stone-50 dark:bg-stone-900 p-6"
      >
        <div className="space-y-1">
          <label htmlFor="signin-email" className="block text-sm font-medium">
            Имейл
          </label>
          <input
            id="signin-email"
            type="email"
            autoComplete="email"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            required
            className="w-full rounded-md border border-black/[0.12] dark:border-white/[0.12] bg-white dark:bg-stone-800 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500"
          />
        </div>

        <div className="space-y-1">
          <label htmlFor="signin-password" className="block text-sm font-medium">
            Парола
          </label>
          <input
            id="signin-password"
            type="password"
            autoComplete="current-password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            required
            className="w-full rounded-md border border-black/[0.12] dark:border-white/[0.12] bg-white dark:bg-stone-800 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-red-500"
          />
        </div>

        {status === "error" && errorMessage ? (
          <p role="alert" className="text-sm text-red-700 dark:text-red-400">
            {errorMessage}
          </p>
        ) : null}

        <button
          type="submit"
          disabled={!canSubmit}
          className="w-full rounded-md bg-red-700 px-4 py-2 text-sm font-medium text-white hover:bg-red-800 disabled:cursor-not-allowed disabled:opacity-50"
        >
          {status === "submitting" ? "Изпращане…" : "Влез"}
        </button>
      </form>

      <div className="flex items-center gap-3 text-xs text-black/55 dark:text-white/55">
        <span className="h-px flex-1 bg-black/[0.12] dark:bg-white/[0.12]" />
        <span>или</span>
        <span className="h-px flex-1 bg-black/[0.12] dark:bg-white/[0.12]" />
      </div>

      <button
        type="button"
        onClick={onGoogleSignIn}
        disabled={status === "submitting"}
        className="w-full rounded-md border border-black/[0.12] dark:border-white/[0.12] bg-white dark:bg-stone-800 px-4 py-2 text-sm font-medium hover:bg-stone-100 dark:hover:bg-stone-700 disabled:cursor-not-allowed disabled:opacity-50"
      >
        Влез с Google
      </button>

      <p className="text-center text-sm text-black/65 dark:text-white/65">
        Нямаш профил?{" "}
        <Link
          href="/sign-up"
          className="hover:underline underline-offset-4 text-red-700 dark:text-red-400"
        >
          Регистрирай се
        </Link>
      </p>
    </div>
  );
}

function translateAuthError(msg: string): string {
  if (/Invalid login credentials/i.test(msg)) return "Невалидни данни за вход.";
  if (/Email not confirmed/i.test(msg))
    return "Имейлът Ви още не е потвърден. Проверете пощата си.";
  if (/Email/i.test(msg)) return "Невалиден имейл адрес.";
  return "Грешка при вход. Моля, опитайте отново.";
}
```

### Step 2.2 — Write `app/sign-in/page.tsx`

```tsx
// File: app/sign-in/page.tsx

import type { Metadata } from "next";
import { SignInForm } from "./sign-in-form";

export const metadata: Metadata = {
  title: "Вход",
  description: "Влезте в профила си в lex.bg.",
};

export default function SignInPage() {
  return (
    <div className="mx-auto max-w-md px-4 py-12 sm:px-6">
      <h1 className="font-serif text-2xl font-semibold tracking-tight text-center mb-6">
        Влезте в профила си
      </h1>
      <SignInForm />
    </div>
  );
}
```

### Step 2.3 — Write `app/auth-nav-link.tsx`

```tsx
// File: app/auth-nav-link.tsx
//
// Navbar auth state link (D-09). Reactive via useSession() (Plan 04-02).
// "Влез" anonymous → /sign-in. "Профил" signed-in → /account (Phase 6
// will populate /account; for Phase 4 the link still works — just lands
// on a 404 until Phase 5+6 land).
//
// Renders nothing while loading to prevent SSR/CSR text flash. The layout
// is a Server Component, so the navbar renders without this link first;
// the client hook then fills in the appropriate label.

"use client";

import Link from "next/link";
import { useSession } from "@/lib/use-session";

export function AuthNavLink() {
  const { user, loading } = useSession();

  if (loading) return null;

  if (user) {
    return (
      <Link
        href="/account"
        className="hover:underline underline-offset-4"
      >
        Профил
      </Link>
    );
  }

  return (
    <Link href="/sign-in" className="hover:underline underline-offset-4">
      Влез
    </Link>
  );
}
```

### Step 2.4 — Modify `app/layout.tsx` to render `<AuthNavLink />`

Use the Edit tool to make TWO precise changes (no other modifications):

1. **Add import** after `import Link from "next/link";` (line 3):
   ```typescript
   import { AuthNavLink } from "./auth-nav-link";
   ```

2. **Append `<AuthNavLink />` as the LAST nav child** (after the `/about` link at lines 106–108). The current last nav link is:
   ```tsx
   <Link href="/about" className="hover:underline underline-offset-4 text-black/65 dark:text-white/65">
     За платформата
   </Link>
   ```

   Append directly after its closing `</Link>`:
   ```tsx
   <AuthNavLink />
   ```

   Visual placement: AuthNavLink lands at the end of the nav row. The `flex-wrap` + `whitespace-nowrap` rules already in `<nav>` mean it'll naturally fall to the right edge on wide screens and wrap on narrow ones — matches D-09's "right-aligned subtle text-link" since the nav uses `sm:justify-between` at the parent.

DO NOT touch any other line in `app/layout.tsx`. The `<header>` structure, the brand link, and all 12 existing nav links must stay byte-identical.

### Step 2.5 — Write `__tests__/sign-in-form.test.tsx`

```tsx
// File: __tests__/sign-in-form.test.tsx

import { describe, expect, it, vi, beforeEach } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";

const signInWithPasswordMock = vi.fn();
const signInWithOAuthMock = vi.fn();
const pushMock = vi.fn();

vi.mock("@/lib/supabase-auth", () => ({
  createBrowserSupabase: () => ({
    auth: {
      signInWithPassword: signInWithPasswordMock,
      signInWithOAuth: signInWithOAuthMock,
    },
  }),
}));

vi.mock("next/navigation", () => ({
  useRouter: () => ({ push: pushMock }),
}));

import { SignInForm } from "@/app/sign-in/sign-in-form";

describe("<SignInForm>", () => {
  beforeEach(() => {
    signInWithPasswordMock.mockReset();
    signInWithOAuthMock.mockReset();
    pushMock.mockReset();
    Object.defineProperty(window, "location", {
      value: { origin: "https://lex-web-eta.vercel.app" },
      configurable: true,
      writable: true,
    });
  });

  it("calls signInWithPassword and routes to / on success", async () => {
    signInWithPasswordMock.mockResolvedValueOnce({ error: null });

    render(<SignInForm />);
    fireEvent.change(screen.getByLabelText(/имейл/i), {
      target: { value: "test@example.bg" },
    });
    fireEvent.change(screen.getByLabelText(/парола/i), {
      target: { value: "secret" },
    });
    fireEvent.click(screen.getByRole("button", { name: /^влез$/i }));

    await waitFor(() => {
      expect(signInWithPasswordMock).toHaveBeenCalledWith({
        email: "test@example.bg",
        password: "secret",
      });
    });
    expect(pushMock).toHaveBeenCalledWith("/");
  });

  it("shows Bulgarian error on invalid credentials", async () => {
    signInWithPasswordMock.mockResolvedValueOnce({
      error: { message: "Invalid login credentials" },
    });

    render(<SignInForm />);
    fireEvent.change(screen.getByLabelText(/имейл/i), {
      target: { value: "wrong@example.bg" },
    });
    fireEvent.change(screen.getByLabelText(/парола/i), {
      target: { value: "bad" },
    });
    fireEvent.click(screen.getByRole("button", { name: /^влез$/i }));

    expect(await screen.findByText(/невалидни данни/i)).toBeInTheDocument();
    expect(pushMock).not.toHaveBeenCalled();
  });

  it("calls signInWithOAuth with provider=google and /auth/callback?next=/", async () => {
    signInWithOAuthMock.mockResolvedValueOnce({ error: null });

    render(<SignInForm />);
    fireEvent.click(screen.getByRole("button", { name: /влез с google/i }));

    await waitFor(() => {
      expect(signInWithOAuthMock).toHaveBeenCalledWith({
        provider: "google",
        options: {
          redirectTo: "https://lex-web-eta.vercel.app/auth/callback?next=/",
        },
      });
    });
  });

  it("renders cross-link to /sign-up", () => {
    render(<SignInForm />);
    const link = screen.getByRole("link", { name: /регистрирай се/i });
    expect(link).toHaveAttribute("href", "/sign-up");
  });
});
```

### Step 2.6 — Write `__tests__/auth-nav-link.test.tsx`

```tsx
// File: __tests__/auth-nav-link.test.tsx

import { describe, expect, it, vi } from "vitest";
import { render, screen } from "@testing-library/react";

const useSessionMock = vi.fn();

vi.mock("@/lib/use-session", () => ({
  useSession: () => useSessionMock(),
}));

import { AuthNavLink } from "@/app/auth-nav-link";

describe("<AuthNavLink>", () => {
  it("renders nothing while loading (prevents SSR/CSR flash)", () => {
    useSessionMock.mockReturnValue({ user: null, loading: true });
    const { container } = render(<AuthNavLink />);
    expect(container).toBeEmptyDOMElement();
  });

  it("renders 'Влез' link to /sign-in for anonymous user", () => {
    useSessionMock.mockReturnValue({ user: null, loading: false });
    render(<AuthNavLink />);
    const link = screen.getByRole("link", { name: /^влез$/i });
    expect(link).toHaveAttribute("href", "/sign-in");
  });

  it("renders 'Профил' link to /account for signed-in user", () => {
    useSessionMock.mockReturnValue({
      user: { id: "u-1", email: "a@b.bg" },
      loading: false,
    });
    render(<AuthNavLink />);
    const link = screen.getByRole("link", { name: /^профил$/i });
    expect(link).toHaveAttribute("href", "/account");
  });

  it("uses hover:underline underline-offset-4 (D-09 navbar pattern)", () => {
    useSessionMock.mockReturnValue({ user: null, loading: false });
    render(<AuthNavLink />);
    const link = screen.getByRole("link");
    expect(link.className).toContain("hover:underline");
    expect(link.className).toContain("underline-offset-4");
  });
});
```

### Step 2.7 — Run all 3 plan-03 vitest files + tsc + build

```bash
cd /Users/beyond/Desktop/lex-web
bun run test -- __tests__/sign-in-form.test.tsx __tests__/sign-up-form.test.tsx __tests__/auth-nav-link.test.tsx
# Expect: 5 + 4 + 4 = 13 passes (sign-up was 5 from Task 1)

# Full suite (no Phase 1/2/8 regressions allowed)
bun run test
npx tsc --noEmit
bun run build
```
  </action>
  <verify>
    <automated>cd /Users/beyond/Desktop/lex-web && test -f app/sign-in/page.tsx && test -f app/sign-in/sign-in-form.tsx && test -f app/auth-nav-link.tsx && grep -q '"use client"' app/sign-in/sign-in-form.tsx && grep -q 'signInWithPassword' app/sign-in/sign-in-form.tsx && grep -q 'signInWithOAuth' app/sign-in/sign-in-form.tsx && grep -q 'provider: "google"' app/sign-in/sign-in-form.tsx && grep -q 'window.location.origin' app/sign-in/sign-in-form.tsx && grep -q 'Влез с Google' app/sign-in/sign-in-form.tsx && grep -q 'Нямаш профил?' app/sign-in/sign-in-form.tsx && grep -q '"use client"' app/auth-nav-link.tsx && grep -q 'useSession' app/auth-nav-link.tsx && grep -q 'hover:underline underline-offset-4' app/auth-nav-link.tsx && grep -q 'AuthNavLink' app/layout.tsx && grep -q 'from "./auth-nav-link"' app/layout.tsx && bun run test -- __tests__/sign-in-form.test.tsx __tests__/auth-nav-link.test.tsx 2>&1 | tail -10 | grep -E 'pass|✓' && npx tsc --noEmit 2>&1 | (! grep -i "error TS") && bun run build 2>&1 | tail -10 | grep -iE 'compiled|ready'</automated>
  </verify>
  <done>
Sign-in form + page shipped; Google OAuth button shape correct; AuthNavLink reactive via useSession; layout.tsx imports + renders AuthNavLink (no other layout changes). All 13 vitest cases (Tasks 1+2) pass. tsc clean. Next 16 build green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING — Live OAuth + email + sign-out manual smoke</name>
  <what-built>
Plans 04-01 + 04-02 + 04-03 Tasks 1+2 are complete:
- DB has user_profiles + RLS + trigger
- @supabase/ssr installed; lib/supabase-auth.ts + lib/use-session.ts ship the factories + hook
- /auth/callback exchanges PKCE codes (with open-redirect guard)
- /api/auth/sign-out clears the cookie
- /sign-up + /sign-in pages render Bulgarian forms with email/password + Google OAuth
- /sign-up/check-email is the magic-link landing
- Navbar shows AuthNavLink (Влез/Профил)

Vitest covers the *invocation shape* of every Supabase Auth call. What vitest CANNOT cover: the actual Google OAuth consent screen (lives at accounts.google.com), real email delivery (Supabase's SMTP), the cookie chunking on real Google tokens (RESEARCH Pitfall 4 — silent prod-only failure), and the Bulgarian rendering of the email template. This is the BLOCKING manual smoke that proves the full flow.
  </what-built>
  <how-to-verify>
**Pre-flight:**
- All previous tasks should report success (5 OK probes from Plan 04-01, 17+ vitest cases green from Plan 04-02, 13 vitest cases green from this plan's Tasks 1+2).
- The pre-implementation operator checklist (Plan 04-01 Task 0) must be done — Google client + Supabase providers configured + Bulgarian email template installed.
- Either run locally (`bun run dev`, http://localhost:3000) OR push to a Vercel preview and use that URL. Local is fine for sign-up email; Vercel preview is needed if testing the production deployment URL specifically.

**Smoke 1 — Google OAuth happy path (AUTH-02):**
1. Open `http://localhost:3000/` (or Vercel preview URL). Confirm navbar shows **"Влез"** at the right edge.
2. Click "Влез" → lands on `/sign-in`.
3. Click **"Влез с Google"**. Browser navigates to `accounts.google.com/...`.
4. Complete the Google consent flow with a real Google account.
5. Browser redirects back to `…/auth/callback?code=…` then to `/`.
6. Confirm navbar now shows **"Профил"** (instead of "Влез").
7. **DB check** — in Supabase SQL Editor (or `psql "$DATABASE_URL"`):
   ```sql
   SELECT id, display_name, created_at
     FROM user_profiles
    WHERE id = (SELECT id FROM auth.users WHERE email = '<your-google-email>');
   ```
   Expected: 1 row, `display_name` = your Google profile name (D-03 — auto-derived from `raw_user_meta_data->>'full_name'`).

**Smoke 2 — Email signup happy path (AUTH-01):**
1. Open `/sign-up` (anonymous — sign out first if needed via Smoke 4 below).
2. Fill in email (a real, deliverable address you control), password (≥6 chars), display name ("Тестов Потребител").
3. Click "Регистрирай се". Browser routes to `/sign-up/check-email`.
4. Check the inbox. Email arrives **in Bulgarian** with subject "Потвърждение на регистрация в lex.bg" (per pre-implementation checklist step 2).
5. Click the confirmation link → browser opens `…/auth/callback?code=…&next=/` → exchanges code → redirects to `/`.
6. Confirm navbar now shows **"Профил"**.
7. **DB check:**
   ```sql
   SELECT id, display_name FROM user_profiles
    WHERE id = (SELECT id FROM auth.users WHERE email = '<your-test-email>');
   ```
   Expected: 1 row, `display_name` = "Тестов Потребител" (D-03 — read from `raw_user_meta_data->>'display_name'`).

**Smoke 3 — Open-redirect guard (RESEARCH Pitfall 3):**
1. Manually visit (while signed in OR anonymous): `http://localhost:3000/auth/callback?next=https://example.com`
2. Confirm browser redirects to `/sign-in?error=callback` (because no `code` was supplied) — NOT to example.com. Either way, the browser MUST stay on lex-web-eta.vercel.app domain.
3. As an additional sanity check: visit `http://localhost:3000/auth/callback?next=//evil.com` (no code). Same expected behavior — stays on local origin.

**Smoke 4 — Sign-out (AUTH-04):**
1. While signed in (navbar shows "Профил"), open DevTools → Application → Cookies → confirm there's an `sb-…-auth-token` cookie.
2. Submit a POST to `/api/auth/sign-out` — easiest from DevTools console:
   ```javascript
   await fetch("/api/auth/sign-out", { method: "POST", redirect: "follow" });
   location.href = "/";
   ```
3. Reload `/`. Navbar reverts to **"Влез"**. The `sb-…-auth-token` cookie is gone.

**Smoke 5 — Phase 6 backward-compatibility (Q5 from RESEARCH):**
1. While anonymous (signed out), visit `/audit/finding/<any-id>` (pick any existing finding).
2. Cast a vote (the existing IP+fingerprint flow).
3. Confirm the vote registered (count ticks up). This proves Phase 4 made ZERO changes to `/api/audit/vote` — the existing anonymous flow is intact.

**What to type back:**
- "approved" — all 5 smokes pass. Include the actual `display_name` values from Smokes 1 and 2 so the SUMMARY can record proof.
- "blocked: <which smoke failed and how>" — describe the failure; replanner will fix.

**Common gotchas (from RESEARCH §"Common Pitfalls" and §"Pre-implementation Operator Checklist"):**
- Smoke 1 fails with "redirect URL not allowed" → Step 5 of operator checklist incomplete (Supabase Site URL + Redirect allow-list)
- Smoke 1 fails with "Auth session missing" in production but works locally → Pitfall 4 (cookie chunking) — verify NOTHING bypasses `setAll` in `lib/supabase-auth.ts` (this should be impossible per the verbatim code in Plan 04-02, but worth a re-grep)
- Smoke 2 email is in English → Step 2 of operator checklist (Bulgarian email template) was not completed
- Smoke 2 succeeds but `display_name` is `<email-localpart>` not "Тестов Потребител" → trigger COALESCE fell through; check that `signUp` is being called with `options.data.display_name` (run `bun run test -- __tests__/sign-up-form.test.tsx` again)
- Smoke 4 returns 405 / 404 → verify `app/api/auth/sign-out/route.ts` exists with `export async function POST`
  </how-to-verify>
  <resume-signal>"approved" once all 5 smokes pass. Paste the two display_name values you saw (Smokes 1 + 2) so SUMMARY records the trigger-COALESCE proof.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Supabase Auth (signUp / signInWithPassword / signInWithOAuth) | Direct browser→Supabase call; lex-web is NOT a man-in-the-middle. Defended by Supabase's own per-IP rate limit + PKCE. |
| Browser ↔ Google OAuth consent (accounts.google.com) | Operator-managed redirect URI allow-list (D-08); attacker cannot register their own callback. |
| Magic-link email → user's mailbox | Supabase SMTP; subject + body customised to Bulgarian per operator checklist. From-address recognition mitigates phishing. |
| Form input → Supabase | Client-side trim + length checks; Supabase server-side validates again (defense in depth). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Spoofing | Phishing via lookalike Bulgarian "Confirm signup" email | mitigate | Operator checklist step 2 sets the Bulgarian template. From-address is operator-controlled in Supabase Dashboard; users will recognize it after the first signup. |
| T-04-16 | Spoofing | Credential stuffing on /sign-in (botnet posts password lists to Supabase Auth) | accept | Sign-in is browser→Supabase direct (lex-web has no surface to wrap). Supabase enforces ~10 sign-in/min/IP by default — sufficient for v2.3 launch. RESEARCH §A2 assumption. |
| T-04-17 | Tampering | App code calls `signUp` without `options.data.display_name` → trigger falls through to email local-part fallback → user has no idea why their name is `john.smith` | mitigate | RESEARCH Pitfall 6. Mitigated by: (a) form requires `displayName` (canSubmit gate); (b) vitest case asserts the call shape includes `options.data.display_name`. |
| T-04-18 | Information Disclosure | XSS in `display_name` rendered in navbar | accept (Phase 4 scope) | Phase 4 navbar does NOT render `display_name` — it renders the literal string "Профил". Phase 6's `/account` page will need to escape `display_name`; that's Phase 6's threat-model, not Phase 4's. |
| T-04-19 | Denial of Service | User creates 1000 throwaway accounts via repeated signUp | accept | Supabase enforces ~30 sign-ups/hour/IP by default. RESEARCH §A2. If abuse manifests post-launch, raise CAPTCHA flag in Supabase dashboard (out of code scope). |
| T-04-20 | Spoofing | Phase 4 introduces a regression in `/api/audit/vote` (anonymous votes) | mitigate | Smoke 5 in Task 3 checkpoint explicitly verifies anonymous vote still works. Q5 in RESEARCH confirmed Phase 4 makes ZERO changes to that route's source. |
| T-04-21 | Tampering | Layout.tsx accidentally drops other nav links during AuthNavLink insertion | mitigate | Action specifies "Use Edit tool to make TWO precise changes (no other modifications)" + lists exact byte-position of insertion. The `bun run build` verification step will fail loudly if any nav link is removed (other tests depend on /audit, /intel routes existing — they'd 500 if the navbar was broken — but more importantly, the file diff will be small + greppable). |

</threat_model>

<verification>
After all three tasks complete:

```bash
cd /Users/beyond/Desktop/lex-web

# 1. All 7 new files + 1 modified file present
for f in app/sign-in/page.tsx app/sign-in/sign-in-form.tsx \
         app/sign-up/page.tsx app/sign-up/sign-up-form.tsx app/sign-up/check-email/page.tsx \
         app/auth-nav-link.tsx; do
  test -f "$f" && echo "OK: $f" || echo "MISSING: $f"
done

# 2. Bulgarian copy present (D-10)
grep -lE 'Влезте в профила|Регистрирайте се|Изпратихме потвърждение' \
  app/sign-in/page.tsx app/sign-up/page.tsx app/sign-up/check-email/page.tsx | wc -l
# Expect: 3

# 3. signUp shape correct (Pitfall 6)
grep -qE 'options.*data.*display_name' app/sign-up/sign-up-form.tsx && echo OK

# 4. signInWithOAuth shape correct (AUTH-02)
grep -qE "provider:\s*[\"']google[\"']" app/sign-in/sign-in-form.tsx && echo OK
grep -q '/auth/callback?next=/' app/sign-in/sign-in-form.tsx && echo OK

# 5. layout.tsx wires AuthNavLink (D-09)
grep -q 'AuthNavLink' app/layout.tsx && echo OK
grep -q 'from "./auth-nav-link"' app/layout.tsx && echo OK

# 6. layout.tsx is otherwise unchanged (sanity check — diff should be small)
git diff --stat app/layout.tsx
# Expect: ~3-5 lines added (1 import + 1 component render + maybe 1 blank). NOT a wholesale rewrite.

# 7. AuthNavLink uses the canonical D-09 hover pattern
grep -q 'hover:underline underline-offset-4' app/auth-nav-link.tsx && echo OK

# 8. All 3 new vitest files green
bun run test -- \
  __tests__/sign-up-form.test.tsx \
  __tests__/sign-in-form.test.tsx \
  __tests__/auth-nav-link.test.tsx

# 9. Full suite green (no Phase 1/2/8 regressions; no Plan 04-02 regressions)
bun run test

# 10. tsc + build green
npx tsc --noEmit
bun run build

# 11. Plan 04-02 lib/supabase.ts STILL untouched (D-07 invariant carries through)
git diff --stat lib/supabase.ts | grep -q 'lib/supabase.ts' && echo "FAIL: regressed D-07" || echo "OK: lib/supabase.ts still untouched"

# 12. /api/audit/vote untouched (Q5 — Phase 6 backward-compat)
git diff --stat app/api/audit/vote/route.ts | grep -q 'audit/vote' && echo "FAIL: regressed Q5" || echo "OK: anonymous vote path untouched"
```
</verification>

<success_criteria>
- All 6 new source files exist and `app/layout.tsx` imports + renders `<AuthNavLink />` with no other modifications.
- `app/sign-up/sign-up-form.tsx`: signUp called with email + password + options.data.display_name + options.emailRedirectTo (greppable + tested).
- `app/sign-in/sign-in-form.tsx`: signInWithPassword + signInWithOAuth (provider=google + redirectTo to /auth/callback?next=/) (greppable + tested).
- `app/auth-nav-link.tsx`: uses useSession, returns null when loading, "Влез" → /sign-in for anonymous, "Профил" → /account for signed-in, with `hover:underline underline-offset-4` (D-09).
- 3 new vitest files: 5 + 4 + 4 = 13 cases minimum. ALL pass.
- Full vitest suite green (Phase 1/2/8/4-prior plans all pass).
- `npx tsc --noEmit` exits 0.
- `bun run build` exits 0.
- D-07 invariant holds: `lib/supabase.ts` still byte-untouched (verifiable via `git diff --stat`).
- Q5 invariant holds: `app/api/audit/vote/route.ts` still byte-untouched.
- BLOCKING manual smoke (Task 3): all 5 smokes pass — Google OAuth, email signup, open-redirect guard, sign-out, anonymous-vote regression test. Operator confirms with "approved" + the two display_name values from Smokes 1+2.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-foundation/04-03-SUMMARY.md` per template, including:
- Test count delta (e.g. "added 13 vitest cases across 3 files; total Phase 4 vitest delta: 30 cases across 7 files")
- Confirmation that AuthNavLink was added with no collateral changes to layout.tsx (paste `git diff --stat app/layout.tsx` showing ~3-5 lines)
- The two `display_name` values observed in Smoke 1 (Google) and Smoke 2 (email signup) — concrete proof the trigger fired and read both metadata keys correctly
- Smoke 3 result (open-redirect guard verified)
- Smoke 5 result (anonymous /audit/vote still works — Q5 invariant)
- Confirmation that BOTH D-07 (lib/supabase.ts untouched) and Q5 (audit/vote untouched) invariants hold across the full Phase 4 diff
- Phase 4 closure: AUTH-01, AUTH-02, AUTH-03, AUTH-04 all marked satisfied; ROADMAP can be flipped to [x]
</output>
