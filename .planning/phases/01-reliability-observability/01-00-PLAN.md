---
phase: 01-reliability-observability
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - /Users/beyond/Desktop/lex-brain/pyproject.toml
  - /Users/beyond/Desktop/lex-brain/uv.lock
  - /Users/beyond/Desktop/lex-web/package.json
  - /Users/beyond/Desktop/lex-web/bun.lock
  - /Users/beyond/Desktop/lex-web/vitest.config.ts
  - /Users/beyond/Desktop/lex-web/tsconfig.json
autonomous: true
requirements: [OS-01, RL-01]

must_haves:
  truths:
    - "psutil is importable from lex-brain test code (uv run python -c 'import psutil' exits 0)"
    - "vitest is runnable from lex-web (bunx vitest --version exits 0)"
    - "lex-web has a vitest.config.ts that wires the jsdom environment and the @/* path alias"
    - "lex-web/package.json exposes a `test` script that runs `vitest run`"
  artifacts:
    - path: "/Users/beyond/Desktop/lex-brain/pyproject.toml"
      provides: "psutil dev dependency"
      contains: 'psutil'
    - path: "/Users/beyond/Desktop/lex-web/vitest.config.ts"
      provides: "vitest config (jsdom env + @/* alias)"
      min_lines: 10
    - path: "/Users/beyond/Desktop/lex-web/package.json"
      provides: "test scripts + vitest devDeps"
      contains: '"test"'
  key_links:
    - from: "/Users/beyond/Desktop/lex-brain/tests/test_opensanctions_memory.py (created later by 01-01)"
      to: "psutil"
      via: "import psutil"
      pattern: "import psutil"
    - from: "/Users/beyond/Desktop/lex-web/__tests__/use-rate-limited-fetch.test.tsx (created later by 01-02)"
      to: "vitest + @testing-library/react"
      via: "import from vitest + @testing-library/react"
      pattern: 'from "@testing-library/react"'

tags:
  - test-infrastructure
  - bootstrap
  - python-deps
  - typescript-deps
---

<objective>
Wave 0 bootstrap. Install the test infrastructure that BOTH Phase 1 plans (01-01 and 01-02) require, in two separate sibling repos. This plan touches only dependency manifests and one new vitest config — zero source code, zero behaviour change.

Purpose: Phase 1 success criteria (peak RSS < 200 MB; rate-limit hook contracts) are only verifiable with `psutil` (lex-brain) and `vitest` + `@testing-library/react` (lex-web). Neither dep is currently installed (RESEARCH §"Environment Availability" — both flagged as "missing, no fallback"). Wave 0 unblocks 01-01 and 01-02 to run in parallel as Wave 1.

Output: `psutil` in lex-brain dev deps; `vitest@4 + @testing-library/react@16 + jsdom + @vitejs/plugin-react` in lex-web devDeps; `vitest.config.ts` wired with jsdom env and `@/*` alias matching `tsconfig.json`.

Out of scope: Writing any test file, modifying any production source file, modifying lock files by hand (uv / bun manage them).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/beyond/Desktop/lex-web/CLAUDE.md
@/Users/beyond/Desktop/lex-web/AGENTS.md
@/Users/beyond/Desktop/lex-web/.planning/PROJECT.md
@/Users/beyond/Desktop/lex-web/.planning/ROADMAP.md
@/Users/beyond/Desktop/lex-web/.planning/phases/01-reliability-observability/01-CONTEXT.md
@/Users/beyond/Desktop/lex-web/.planning/phases/01-reliability-observability/01-RESEARCH.md

<interfaces>
<!-- Existing manifests the executor must preserve. Read before modifying. -->

From /Users/beyond/Desktop/lex-brain/pyproject.toml:
- `requires-python = ">=3.12"` (already set)
- `httpx>=0.28.1` already in main `dependencies`
- `[dependency-groups]` table EXISTS (line 27); `dev` group is the canonical home for test-only deps
- `uv` is the package manager; `uv add --dev <pkg>` is the canonical add command — DO NOT hand-edit `uv.lock`

From /Users/beyond/Desktop/lex-web/package.json:
- `"name": "lex-web"`, `"version": "0.1.0"`
- `"scripts"` block exists (line 5); add a `"test"` and `"test:watch"` entry
- `"devDependencies"` block exists (line 19); ZERO test framework deps currently present
- Bun is the package manager (`bun.lock` is the lockfile); `bun add -D <pkg>` is the canonical add command — DO NOT hand-edit `bun.lock`
- React 19.2.4 + Next 16.2.4 are pinned in deps

From /Users/beyond/Desktop/lex-web/tsconfig.json:
- `@/*` path alias maps to project root (used by every existing import in the codebase, e.g. `@/lib/rate-limit`)
- The vitest config MUST mirror this alias or test files cannot import from `@/lib/...`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add psutil to lex-brain dev dependency group</name>
  <files>/Users/beyond/Desktop/lex-brain/pyproject.toml, /Users/beyond/Desktop/lex-brain/uv.lock</files>
  <read_first>
    - /Users/beyond/Desktop/lex-brain/pyproject.toml (verify current `[dependency-groups]` shape; do NOT touch other groups)
  </read_first>
  <action>
    Run, with absolute working directory:
    ```
    cd /Users/beyond/Desktop/lex-brain && uv add --dev psutil
    ```
    This will (a) add `psutil` to the `[dependency-groups].dev` array in `pyproject.toml` and (b) regenerate `uv.lock`. DO NOT hand-edit either file. The version pin will be uv's default — accept it (psutil 7.x is current per RESEARCH §"Standard Stack — Track A"). Per D-14, this is the dependency that backs the `psutil.Process().memory_info().rss` peak-RSS sampler used by the OS-01 memory test.

    Then verify:
    ```
    cd /Users/beyond/Desktop/lex-brain && uv run python -c "import psutil; print(psutil.__version__)"
    ```
    The version line must print to stdout with exit 0.
  </action>
  <verify>
    <automated>cd /Users/beyond/Desktop/lex-brain && uv run python -c "import psutil; print(psutil.__version__)" && grep -q "psutil" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^[[:space:]]*"psutil' /Users/beyond/Desktop/lex-brain/pyproject.toml` returns at least 1
    - `cd /Users/beyond/Desktop/lex-brain && uv run python -c "import psutil"` exits 0
    - `/Users/beyond/Desktop/lex-brain/uv.lock` modification time is newer than before the task ran (i.e. lockfile regenerated)
    - No other `[dependency-groups]` entries removed (diff `pyproject.toml` against HEAD shows ONLY a psutil addition under `dev`)
  </acceptance_criteria>
  <done>psutil installed as a dev dep in lex-brain via uv; importable from a uv-run Python invocation; uv.lock updated by uv (not by hand).</done>
</task>

<task type="auto">
  <name>Task 2: Add vitest + RTL + jsdom + @vitejs/plugin-react to lex-web devDeps and add test scripts</name>
  <files>/Users/beyond/Desktop/lex-web/package.json, /Users/beyond/Desktop/lex-web/bun.lock</files>
  <read_first>
    - /Users/beyond/Desktop/lex-web/package.json (full file — preserve every existing script and devDependency)
  </read_first>
  <action>
    Per RESEARCH §"Environment Availability" + §"Wave 0 Gaps", add the test stack as devDependencies via Bun (the project package manager — do NOT use npm/pnpm/yarn). Run, with absolute cwd:
    ```
    cd /Users/beyond/Desktop/lex-web && bun add -D vitest @vitest/ui @testing-library/react @testing-library/dom @testing-library/jest-dom jsdom @vitejs/plugin-react @types/react
    ```
    `@types/react` may already be present — Bun is idempotent here; the install should succeed regardless. The jest-dom matchers (`@testing-library/jest-dom`) are recommended by Testing Library docs for the `toBeInTheDocument` family used in any future component tests.

    Then add two scripts to `package.json` `"scripts"` (preserve every existing script — only ADD these two keys):
    ```json
    "test": "vitest run",
    "test:watch": "vitest"
    ```

    Use the Edit tool to add the script keys (do NOT rewrite the whole file). After Bun finishes the install, verify the scripts and binary:
    ```
    cd /Users/beyond/Desktop/lex-web && bunx vitest --version
    ```
    Must print a version (e.g. `vitest/4.x.x`) with exit 0.
  </action>
  <verify>
    <automated>cd /Users/beyond/Desktop/lex-web && bunx vitest --version && grep -q '"vitest"' package.json && grep -q '"test"[[:space:]]*:[[:space:]]*"vitest run"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"vitest"' /Users/beyond/Desktop/lex-web/package.json` returns at least 1 (under devDependencies)
    - `grep -c '"@testing-library/react"' /Users/beyond/Desktop/lex-web/package.json` returns at least 1
    - `grep -c '"jsdom"' /Users/beyond/Desktop/lex-web/package.json` returns at least 1
    - `grep -c '"@vitejs/plugin-react"' /Users/beyond/Desktop/lex-web/package.json` returns at least 1
    - `grep -c '"test"[[:space:]]*:[[:space:]]*"vitest run"' /Users/beyond/Desktop/lex-web/package.json` returns 1
    - `grep -c '"test:watch"[[:space:]]*:[[:space:]]*"vitest"' /Users/beyond/Desktop/lex-web/package.json` returns 1
    - `cd /Users/beyond/Desktop/lex-web && bunx vitest --version` exits 0
    - `/Users/beyond/Desktop/lex-web/bun.lock` modification time is newer than before the task ran
    - No existing devDependency removed (run `git diff package.json` before commit; only additions for the new deps + the two script keys should appear)
  </acceptance_criteria>
  <done>vitest 4.x + RTL + jsdom + plugin-react installed as devDeps; `test` and `test:watch` scripts present; `bunx vitest --version` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Create lex-web vitest.config.ts with jsdom env and @/* path alias</name>
  <files>/Users/beyond/Desktop/lex-web/vitest.config.ts</files>
  <read_first>
    - /Users/beyond/Desktop/lex-web/tsconfig.json (read the `paths` block — the vitest alias MUST resolve `@/*` the same way the TypeScript compiler does, otherwise `import { useRateLimitedFetch } from "@/lib/use-rate-limited-fetch"` will fail in tests but pass in production)
    - /Users/beyond/Desktop/lex-web/package.json (confirm vitest is in devDependencies before writing the config)
  </read_first>
  <action>
    Create `/Users/beyond/Desktop/lex-web/vitest.config.ts` with the EXACT content below. The `path.resolve(__dirname, ".")` form mirrors how Next 16 + tsconfig.json paths resolve `@/*` to project root. `environment: "jsdom"` is required because the React 19 hook tests in 01-02 use `renderHook` which needs a DOM. `globals: true` allows `describe`/`it`/`expect` without explicit imports (matches Vitest convention used by RESEARCH §"Vitest hook test skeleton").

    ```ts
    import { defineConfig } from "vitest/config";
    import react from "@vitejs/plugin-react";
    import path from "node:path";

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: "jsdom",
        globals: true,
        setupFiles: [],
        include: ["__tests__/**/*.{test,spec}.{ts,tsx}", "lib/**/*.{test,spec}.{ts,tsx}"],
      },
      resolve: {
        alias: {
          "@": path.resolve(__dirname, "."),
        },
      },
    });
    ```

    After writing, smoke-test the config (no test files exist yet — vitest should report "No test files found" but exit cleanly with config-discovery success):
    ```
    cd /Users/beyond/Desktop/lex-web && bunx vitest run --reporter=basic 2>&1 | head -20
    ```
    The output must NOT contain "Cannot find module", "Failed to resolve", or "ConfigError". Exit code may be non-zero (no tests found is treated as failure by some vitest versions); the criterion is config discovery, not zero exit.

    Per AGENTS.md: this is Next.js 16 + React 19; the config above was verified against `node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md` — no Next 16 specifics affect vitest config.
  </action>
  <verify>
    <automated>test -f /Users/beyond/Desktop/lex-web/vitest.config.ts && cd /Users/beyond/Desktop/lex-web && bunx vitest run --reporter=basic 2>&1 | grep -vE "Cannot find module|Failed to resolve|ConfigError" >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/beyond/Desktop/lex-web/vitest.config.ts` exists
    - `grep -c 'environment:[[:space:]]*"jsdom"' /Users/beyond/Desktop/lex-web/vitest.config.ts` returns 1
    - `grep -c 'plugins:[[:space:]]*\[react()\]' /Users/beyond/Desktop/lex-web/vitest.config.ts` returns 1
    - `grep -c '"@":' /Users/beyond/Desktop/lex-web/vitest.config.ts` returns 1
    - `grep -c 'include:' /Users/beyond/Desktop/lex-web/vitest.config.ts` returns 1
    - `cd /Users/beyond/Desktop/lex-web && bunx vitest run --reporter=basic 2>&1` does NOT contain the strings "Cannot find module", "Failed to resolve", or "ConfigError"
    - `wc -l < /Users/beyond/Desktop/lex-web/vitest.config.ts` returns at least 10
  </acceptance_criteria>
  <done>vitest.config.ts exists, configures jsdom + react plugin + `@/*` alias; vitest config-discovery smoke test passes (no module-resolution errors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → dev machine | `bun add -D` and `uv add --dev` fetch packages from npm + PyPI; supply-chain integrity is the default-trust model for both |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-00-01 | Tampering | Dependency manifests (pyproject.toml, package.json) | mitigate | Use ONLY the official package managers (`uv add --dev`, `bun add -D`) — never hand-edit lockfiles. Both managers verify package integrity hashes against their respective registries' indexes. |
| T-01-00-02 | Spoofing | Newly-installed transitive deps from npm/PyPI | accept | Standard registry trust model; same trust profile as every existing dep. No new attack surface beyond what the project already accepts. Bun + uv both pin transitive hashes in lockfiles. |
| T-01-00-03 | Information disclosure | Test config exposing source paths | accept | `vitest.config.ts` contains only the project root path alias and a glob pattern — no secrets, no credentials. Same exposure as `tsconfig.json` already provides. |

No high-severity threats. This is dev-tooling-only and ships no production code.
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# lex-brain side
cd /Users/beyond/Desktop/lex-brain && \
  uv run python -c "import psutil; print(psutil.__version__)" && \
  grep -q '"psutil' pyproject.toml

# lex-web side
cd /Users/beyond/Desktop/lex-web && \
  bunx vitest --version && \
  test -f vitest.config.ts && \
  grep -q '"vitest"' package.json && \
  grep -q '"test"[[:space:]]*:[[:space:]]*"vitest run"' package.json
```

All commands must exit 0.
</verification>

<success_criteria>
1. lex-brain: `uv run python -c "import psutil"` exits 0; `pyproject.toml` lists psutil under `[dependency-groups].dev`.
2. lex-web: `bunx vitest --version` prints a version with exit 0.
3. lex-web: `vitest.config.ts` exists with jsdom env + react plugin + `@/*` alias resolving to project root.
4. lex-web: `package.json` has `"test": "vitest run"` and `"test:watch": "vitest"` script entries.
5. Both lockfiles (`uv.lock`, `bun.lock`) regenerated by their respective managers (modification times newer than file existed before the plan ran).
6. Zero source files modified; zero existing scripts or deps removed.
</success_criteria>

<output>
After completion, create `/Users/beyond/Desktop/lex-web/.planning/phases/01-reliability-observability/01-00-SUMMARY.md` with:
- The exact installed versions of `psutil`, `vitest`, `@testing-library/react`, `jsdom`, `@vitejs/plugin-react`
- The vitest config decisions (env=jsdom, alias, include globs)
- A confirmation that 01-01 and 01-02 can now run in parallel as Wave 1
</output>
</content>
