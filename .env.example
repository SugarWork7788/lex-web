NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx

# /admin dashboard gate — only this email may view operations stats.
ADMIN_EMAIL=
# Where the lex-brain scrapers write logs (host-only; unused on Vercel).
LEX_BRAIN_LOGS_DIR=
# GitHub API access for CI status + recent PRs panel (optional).
GITHUB_TOKEN=
GITHUB_REPO=owner/repo
